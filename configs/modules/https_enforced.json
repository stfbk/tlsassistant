{
   "input":[
      {
         "name":"hostname",
         "type":"str",
         "description":"Hostname to analyze",
         "required":"True"
      }
   ],
   "description":"This modules checks if the hostname HTTPS is enforced.",
   "path":"modules/server/https_not_enforced.py",
   "class_name":"Https_not_enforced",
   "output":[
      {
         "name":"results",
         "type":"dict",
         "description":"results of the scan"
      }
   ]
}