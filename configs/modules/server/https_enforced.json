{
   "input":[
      {
         "name":"hostname",
         "type":"str",
         "description":"Hostname to analyze",
         "required":"True"
      }
   ],
   "description":"This modules checks if the hostname HTTPS is enforced.",
   "path":"modules/server/https_enforced.py",
   "class_name":"Https_enforced",
   "output":[
      {
         "name":"results",
         "type":"dict",
         "description":"results of the scan"
      }
   ]
}